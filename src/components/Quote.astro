---
// Quote.astro: Componente para citas en el blog

// No recibe props directos, solo slot.
---

<blockquote class="quote">
  <slot />
</blockquote>
<style>
  .quote {
    border-left: 5px solid var(--primary-color, #00d4ff);
    background: rgba(22, 33, 62, 0.85); /* fondo oscuro translúcido */
    color: var(--text-primary, #fff) !important;
    padding: 1.5em 2em;
    margin: 2em 0;
    font-style: italic;
    border-radius: 1em;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.18);
    font-size: 1.25em;
    line-height: 1.7;
    font-weight: 500;
    letter-spacing: 0.01em;
    transition: background 0.3s;
  }
  .quote strong {
    color: var(--text-primary, #fff);
    font-weight: 700;
  }
</style>
