---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ContactForm from '../components/ContactForm.astro';

const contactMethods = [
  {
    icon: '📧',
    title: 'Email',
    value: 'chrystianmichell@hotmail.com',
    link: 'mailto:chrystianmichell@hotmail.com',
    description: 'Respuesta en 24-48 horas',
  },
  {
    icon: 'mobile',
    title: 'Teléfono',
    value: '+34 653 31 69 44',
    link: 'tel:+34653316944',
    description: 'Llamadas y WhatsApp',
  },
  {
    icon: '💼',
    title: 'LinkedIn',
    value: 'chrystianmichell',
    link: 'https://linkedin.com/in/chrystianmichell',
    description: 'Conectemos profesionalmente',
  },
  {
    icon: '💻',
    title: 'GitHub',
    value: 'chrystian-dev',
    link: 'https://github.com/chrystian-dev',
    description: 'Revisa mi código',
  },
];

const services = [
  'Desarrollo de aplicaciones móviles multiplataforma con Flutter',
  'Arquitectura de software y liderazgo técnico',
  'Consultoría en desarrollo multiplataforma y optimización de rendimiento',
  'Migración y modernización de aplicaciones legacy',
  'Implementación de CI/CD y DevOps para equipos de desarrollo',
  'Mentoría técnica y formación de equipos de desarrollo',
  'Integración con sistemas PMS hoteleros y APIs de terceros',
  'Desarrollo de plataformas OTT y sistemas de streaming',
];
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Contacta con Chrystian Michell para proyectos de desarrollo Flutter, liderazgo técnico, arquitectura de software y consultoría en desarrollo multiplataforma."
    />
    <title>Contacto | Chrystian Michell - Líder Técnico Flutter</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <Header />

    <main>
      <!-- Hero Section -->
      <section class="hero-contact">
        <div class="container">
          <div class="hero-content">
            <h1 class="hero-title">Trabajemos Juntos</h1>
            <p class="hero-description">
              ¿Necesitas un líder técnico para tu equipo? ¿Tienes un proyecto
              desafiante que requiere expertise en Flutter y arquitecturas
              escalables? Hablemos sobre cómo puedo ayudarte a llevarlo al
              siguiente nivel.
            </p>
          </div>
        </div>
      </section>

      <!-- Contact Methods -->
      <section class="contact-methods">
        <div class="container">
          <h2 class="section-title">Formas de contacto</h2>
          <div class="contact-grid">
            {
              contactMethods.map((method) => (
                <a
                  href={method.link}
                  class="contact-method"
                  target={method.link.startsWith('http') ? '_blank' : ''}
                  rel={
                    method.link.startsWith('http') ? 'noopener noreferrer' : ''
                  }
                >
                  <div class="contact-icon">{method.icon}</div>
                  <h3>{method.title}</h3>
                  <p class="contact-value">{method.value}</p>
                  <p class="contact-description">{method.description}</p>
                </a>
              ))
            }
          </div>
        </div>
      </section>

      <!-- Services -->
      <section class="services-section">
        <div class="container">
          <h2 class="section-title">¿En qué puedo ayudarte?</h2>
          <div class="services-grid">
            {
              services.map((service) => (
                <div class="service-item">
                  <div class="service-icon">✅</div>
                  <p>{service}</p>
                </div>
              ))
            }
          </div>
        </div>
      </section>

      <!-- Contact Form -->
      <section class="contact-form-section">
        <div class="container">
          <div class="form-container">
            <div class="form-header">
              <h2>Envíame un mensaje</h2>
              <p>
                Cuéntame sobre tu proyecto y te responderé lo antes posible.
                Todas las consultas son confidenciales.
              </p>
            </div>
            <ContactForm />
          </div>
        </div>
      </section>

      <!-- FAQ -->
      <section class="faq-section">
        <div class="container">
          <h2 class="section-title">Preguntas frecuentes</h2>
          <div class="faq-grid">
            <div class="faq-item">
              <h3>¿Cuánto tiempo toma desarrollar una app?</h3>
              <p>
                Depende de la complejidad. Una app básica puede tomar 2-3 meses,
                mientras que aplicaciones más complejas pueden requerir 6-12
                meses.
              </p>
            </div>
            <div class="faq-item">
              <h3>¿Trabajas con equipos remotos?</h3>
              <p>
                Sí, tengo experiencia liderando equipos distribuidos
                globalmente. Uso herramientas modernas de colaboración y
                metodologías ágiles.
              </p>
            </div>
            <div class="faq-item">
              <h3>¿Ofreces soporte post-lanzamiento?</h3>
              <p>
                Absolutamente. Ofrezco planes de mantenimiento y soporte
                continuo para asegurar el éxito a largo plazo de tu aplicación.
              </p>
            </div>
            <div class="faq-item">
              <h3>¿Qué incluye la consultoría técnica?</h3>
              <p>
                Análisis de arquitectura, revisión de código, optimización de
                rendimiento, implementación de mejores prácticas y mentoría del
                equipo.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- CTA -->
      <section class="cta-contact">
        <div class="container">
          <div class="cta-content">
            <h2>¿Listo para empezar?</h2>
            <p>
              No esperes más. Convirtamos tu idea en el próximo producto digital
              exitoso.
            </p>
            <div class="hero-cta">
              <a
                href="mailto:chrystianmichell@hotmail.com?subject=Propuesta de proyecto"
                class="btn btn-primary"
              >
                📧 Enviar email
              </a>
              <a
                href="https://calendly.com/chrystianmichell"
                class="btn btn-outline"
                target="_blank"
                rel="noopener"
              >
                📅 Agendar reunión
              </a>
            </div>
          </div>
        </div>
      </section>
    </main>

    <Footer />

    <style>
      .hero-contact {
        padding: 8rem 0 4rem 0;
        background: var(--gradient-secondary);
        text-align: center;
      }

      .hero-contact .hero-title {
        margin-bottom: 1.5rem;
        background: linear-gradient(
          135deg,
          var(--text-primary) 0%,
          var(--primary-color) 100%
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .hero-contact .hero-description {
        font-size: 1.2rem;
        max-width: 700px;
        margin: 0 auto;
        color: var(--text-secondary);
      }

      .contact-methods {
        padding: 5rem 0;
        background: var(--background-card);
      }

      .contact-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
      }

      .contact-method {
        background: var(--gradient-secondary);
        padding: 2.5rem 2rem;
        border-radius: var(--border-radius);
        text-align: center;
        border: 1px solid rgba(0, 212, 255, 0.1);
        transition: var(--transition);
        text-decoration: none;
        color: inherit;
      }

      .contact-method:hover {
        transform: translateY(-10px);
        box-shadow: var(--shadow-light);
        border-color: var(--primary-color);
      }

      .contact-method .contact-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
      }

      .contact-method h3 {
        color: var(--text-primary);
        margin-bottom: 1rem;
      }

      .contact-value {
        color: var(--primary-color);
        font-weight: 600;
        font-size: 1.1rem;
        margin-bottom: 0.5rem;
      }

      .contact-description {
        color: var(--text-muted);
        font-size: 0.9rem;
      }

      .services-section {
        padding: 5rem 0;
      }

      .services-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
        margin-top: 3rem;
      }

      .service-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1.5rem;
        background: var(--background-card);
        border-radius: var(--border-radius);
        border: 1px solid rgba(0, 212, 255, 0.1);
        transition: var(--transition);
      }

      .service-item:hover {
        transform: translateY(-3px);
        box-shadow: var(--shadow-light);
      }

      .service-icon {
        font-size: 1.5rem;
        color: var(--primary-color);
      }

      .service-item p {
        margin: 0;
        color: var(--text-secondary);
      }

      .contact-form-section {
        padding: 5rem 0;
        background: var(--background-card);
      }

      .form-container {
        max-width: 800px;
        margin: 0 auto;
      }

      .form-header {
        text-align: center;
        margin-bottom: 3rem;
      }

      .form-header h2 {
        color: var(--text-primary);
        margin-bottom: 1rem;
      }

      .form-header p {
        color: var(--text-secondary);
        font-size: 1.1rem;
      }

      .faq-section {
        padding: 5rem 0;
      }

      .faq-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
      }

      .faq-item {
        padding: 2rem;
        background: var(--background-card);
        border-radius: var(--border-radius);
        border: 1px solid rgba(0, 212, 255, 0.1);
        transition: var(--transition);
      }

      .faq-item:hover {
        transform: translateY(-5px);
        box-shadow: var(--shadow-light);
      }

      .faq-item h3 {
        color: var(--text-primary);
        margin-bottom: 1rem;
        font-size: 1.2rem;
      }

      .faq-item p {
        color: var(--text-secondary);
        line-height: 1.6;
      }

      .cta-contact {
        padding: 5rem 0;
        background: var(--gradient-secondary);
        text-align: center;
      }

      .cta-contact h2 {
        color: var(--text-primary);
        margin-bottom: 1.5rem;
        font-size: 2.5rem;
      }

      .cta-contact p {
        color: var(--text-secondary);
        font-size: 1.2rem;
        max-width: 600px;
        margin: 0 auto 3rem auto;
      }

      /* Mobile Responsive */
      @media (max-width: 768px) {
        .hero-contact {
          padding: 6rem 0 3rem 0;
        }

        .contact-grid {
          grid-template-columns: 1fr;
        }

        .services-grid {
          grid-template-columns: 1fr;
        }

        .faq-grid {
          grid-template-columns: 1fr;
        }

        .contact-methods,
        .services-section,
        .contact-form-section,
        .faq-section,
        .cta-contact {
          padding: 3rem 0;
        }

        .cta-contact h2 {
          font-size: 2rem;
        }
      }
    </style>
  </body>
</html>
