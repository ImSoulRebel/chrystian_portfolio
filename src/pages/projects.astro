---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ProjectCard from '../components/ProjectCard.astro';

const projects = [
  {
    title: 'YUVOD – Plataforma OTT para Hoteles',
    stack: 'Flutter · BLoC · WebSockets · Video.js · Docker · GitHub Actions',
    description:
      'Plataforma de streaming completa para SmartTVs, móviles y web con navegación espacial custom, reproductor modular con DRM, sistema de usuarios y panel de administración. Arquitectura escalable con microservicios y selección dinámica de UI para diferentes dispositivos.',
    impact:
      'Desplegada en +30 hoteles en Europa y LATAM. Redujo errores de navegación un 70% y soporta +1000 usuarios concurrentes. Mejora del 35% en satisfacción de huéspedes.',
    featured: true,
    githubUrl: 'https://github.com/chrystian-dev',
    liveUrl: 'https://yuvod.com',
  },
  {
    title: 'Nextival CMS – Gestión de Festivales',
    stack: 'Astro · SolidJS · TailwindCSS · Node.js · WebSockets',
    description:
      'Sistema de gestión de contenido para festivales musicales con renderizado híbrido SSR/SSG, Clean Architecture con Repository Pattern, integración con Spotify Web API, editor WYSIWYG avanzado y sistema de roles para diferentes tipos de usuarios.',
    impact:
      'Optimización SEO completa y tiempos de carga <1.5s. Entregado en 4 meses como proyecto funcional para producción. Capacidad para gestionar +50 festivales simultáneamente.',
    githubUrl: 'https://github.com/chrystian-dev',
  },
  {
    title: 'STELCAMPING – Gestión Integral de Campings',
    stack: 'Flutter · Firebase · GetX · REST APIs · Push Notifications',
    description:
      'Aplicación integral para gestión de campings con sistema de reservas, calendario de actividades, notificaciones push, chat en tiempo real, pagos integrados y panel administrativo completo. Arquitectura modular con clase base BaseState<T> para centralizar utilidades.',
    impact:
      'Automatizó procesos reduciendo costes operativos en 40%. +200 campings registrados. Más de 1,000 usuarios activos y procesó +€500K en reservas.',
    githubUrl: 'https://github.com/chrystian-dev',
    liveUrl: 'https://stel.es',
  },
  {
    title: 'BNC10 – Aplicación Neobanca Digital',
    stack: 'Flutter · GetX · Firebase · Biometric Auth · Sentry',
    description:
      'Aplicación de banca digital con autenticación biométrica, transferencias instantáneas, monitoreo en tiempo real, sistema de tarjetas virtuales y análisis financiero personal. Responsive avanzado con responsive_framework y monitoreo de errores con Sentry.',
    impact:
      '500+ usuarios activos, rating 4.7★, 99.5% sesiones sin crashes. Procesó +€2M en transacciones. Reducción del 60% en fricción de autenticación vs solución anterior.',
    githubUrl: 'https://github.com/chrystian-dev',
  },
  {
    title: 'TOOKANE – Sistema de Seguimiento Logístico',
    stack: 'Flutter · Riverpod · Supabase · Geolocalización Real-Time',
    description:
      'Plataforma logística con geolocalización en tiempo real, soporte offline-first, sincronización automática, optimización de rutas y dashboard de análisis completo. Clean Architecture + MVVM + Repository Pattern con optimización de batería.',
    impact:
      'Mejora del 30% en eficiencia de entregas. Redujo costes de combustible en 25%. Optimización de batería reduciendo consumo en 40%.',
    githubUrl: 'https://github.com/chrystian-dev',
    liveUrl: 'https://tookane.com',
  },
  {
    title: 'E-Commerce Flutter Completo',
    stack: 'Flutter · Provider · Stripe · Firebase · Push Notifications',
    description:
      'Aplicación de comercio electrónico completa con carrito de compras, pagos seguros con Stripe, sistema de reseñas y valoraciones, wishlist, notificaciones push, panel de vendedor y analytics de ventas en tiempo real.',
    impact:
      'Procesó +€500K en ventas. Rating 4.8★ en stores. +10,000 productos gestionados.',
    githubUrl: 'https://github.com/chrystian-dev',
  },
  {
    title: 'Task Manager Pro – Gestión de Proyectos',
    stack: 'React · TypeScript · Node.js · MongoDB · Socket.io',
    description:
      'Gestor de proyectos y tareas con colaboración en tiempo real, asignación de roles, chat integrado, reportes avanzados, integraciones con herramientas externas (Slack, Trello) y dashboard de productividad.',
    impact:
      'Adoptado por +50 equipos. Mejora del 40% en productividad. +5,000 tareas gestionadas diariamente.',
    githubUrl: 'https://github.com/chrystian-dev',
  },
  {
    title: 'Hotel Management System',
    stack: 'Flutter · Firebase · PMS Integration · Biometric Auth',
    description:
      'Sistema integral de gestión hotelera con check-in digital, control de llaves móvil, servicios a la habitación, integración PMS (Oracle OPERA, Mews), análisis de ocupación y dashboard de análisis operativo.',
    impact:
      'Implementado en +20 hoteles. Redujo tiempo de check-in en 60%. Mejora del 45% en eficiencia operativa.',
    githubUrl: 'https://github.com/chrystian-dev',
  },
];

const categories = [
  'Todos',
  'Flutter',
  'Web Development',
  'E-Commerce',
  'Sistemas',
  'Streaming',
];
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Portfolio de proyectos de Chrystian Michell - Líder Técnico & Arquitecto Flutter. Apps móviles, plataformas OTT, sistemas empresariales en 11 países."
    />
    <title>Proyectos | Chrystian Michell - Líder Técnico Flutter</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <Header />

    <main>
      <!-- Hero Section -->
      <section class="hero-projects">
        <div class="container">
          <div class="hero-content">
            <h1 class="hero-title">📂 Portfolio de Proyectos</h1>
            <p class="hero-description">
              Una selección de mis mejores trabajos en desarrollo
              multiplataforma y liderazgo técnico. Desde plataformas OTT
              desplegadas en 11 países hasta aplicaciones neobanca con más de
              500 usuarios activos.
            </p>

            <!-- Filter Categories -->
            <div class="project-filters">
              {
                categories.map((category) => (
                  <button
                    class="filter-btn active"
                    data-filter={category.toLowerCase()}
                  >
                    {category}
                  </button>
                ))
              }
            </div>
          </div>
        </div>
      </section>

      <!-- Projects Grid -->
      <section class="projects-showcase">
        <div class="container">
          <div class="projects-grid" id="projects-grid">
            {projects.map((project) => <ProjectCard {...project} />)}
          </div>

          <!-- Load More Button -->
          <div class="load-more-container">
            <button class="btn btn-outline load-more-btn">
              Ver más proyectos
            </button>
          </div>
        </div>
      </section>

      <!-- Call to Action -->
      <section class="cta-section">
        <div class="container">
          <div class="cta-content">
            <h2>¿Tienes un proyecto en mente?</h2>
            <p>
              Me especializo en convertir ideas innovadoras en productos
              digitales exitosos. ¡Hablemos sobre tu próximo proyecto!
            </p>
            <div class="hero-cta">
              <a href="#contact" class="btn btn-primary"
                >💬 Iniciar conversación</a
              >
              <a
                href="mailto:chrystianmichell@hotmail.com"
                class="btn btn-outline">📧 Enviar email</a
              >
            </div>
          </div>
        </div>
      </section>
    </main>

    <Footer />

    <style>
      .hero-projects {
        padding: 8rem 0 4rem 0;
        background: var(--gradient-secondary);
        text-align: center;
      }

      .hero-projects .hero-title {
        margin-bottom: 1.5rem;
        background: linear-gradient(
          135deg,
          var(--text-primary) 0%,
          var(--primary-color) 100%
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .hero-projects .hero-description {
        font-size: 1.2rem;
        max-width: 700px;
        margin: 0 auto 3rem auto;
        color: var(--text-secondary);
      }

      .project-filters {
        display: flex;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
        margin-top: 2rem;
      }

      .filter-btn {
        padding: 0.8rem 1.5rem;
        background: transparent;
        color: var(--text-secondary);
        border: 2px solid rgba(0, 212, 255, 0.3);
        border-radius: 25px;
        cursor: pointer;
        transition: var(--transition);
        font-weight: 500;
      }

      .filter-btn:hover,
      .filter-btn.active {
        background: var(--gradient-primary);
        color: var(--background-dark);
        border-color: var(--primary-color);
        transform: translateY(-2px);
      }

      .projects-showcase {
        padding: 4rem 0;
        background: var(--background-card);
      }

      .load-more-container {
        text-align: center;
        margin-top: 4rem;
      }

      .load-more-btn {
        padding: 1rem 2rem;
      }

      .cta-section {
        padding: 5rem 0;
        background: var(--gradient-secondary);
        text-align: center;
      }

      .cta-content h2 {
        color: var(--text-primary);
        margin-bottom: 1.5rem;
        font-size: 2.5rem;
      }

      .cta-content p {
        font-size: 1.2rem;
        color: var(--text-secondary);
        max-width: 600px;
        margin: 0 auto 3rem auto;
      }

      /* Mobile Responsive */
      @media (max-width: 768px) {
        .hero-projects {
          padding: 6rem 0 3rem 0;
        }

        .project-filters {
          gap: 0.5rem;
        }

        .filter-btn {
          padding: 0.6rem 1rem;
          font-size: 0.9rem;
        }

        .projects-showcase {
          padding: 3rem 0;
        }

        .cta-content h2 {
          font-size: 2rem;
        }
      }
    </style>

    <script>
      // Project filtering functionality
      const filterButtons = document.querySelectorAll('.filter-btn');
      const projectCards = document.querySelectorAll('.project-card');

      filterButtons.forEach((button) => {
        button.addEventListener('click', () => {
          // Remove active class from all buttons
          filterButtons.forEach((btn) => btn.classList.remove('active'));

          // Add active class to clicked button
          button.classList.add('active');

          const btnElement = button as HTMLElement;
          const filter = btnElement.dataset.filter;

          // Show/hide projects based on filter
          projectCards.forEach((card) => {
            const cardElement = card as HTMLElement;
            if (filter === 'todos') {
              cardElement.style.display = 'block';
              cardElement.style.animation = 'fadeIn 0.5s ease';
            } else {
              const projectTechElement = card.querySelector('.project-tech');
              const projectTech =
                projectTechElement?.textContent?.toLowerCase() || '';
              if (projectTech.includes(filter || '')) {
                cardElement.style.display = 'block';
                cardElement.style.animation = 'fadeIn 0.5s ease';
              } else {
                cardElement.style.display = 'none';
              }
            }
          });
        });
      });

      // Load more functionality (placeholder)
      const loadMoreBtn = document.querySelector('.load-more-btn');
      loadMoreBtn?.addEventListener('click', () => {
        // In a real application, this would load more projects
        alert(
          'Funcionalidad de "cargar más" - En una aplicación real cargaría más proyectos',
        );
      });

      // Smooth scrolling for links
      document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
        anchor.addEventListener('click', function (this: HTMLAnchorElement, e) {
          e.preventDefault();
          const href = this.getAttribute('href');
          const target = document.querySelector(href || '');
          if (target) {
            target.scrollIntoView({
              behavior: 'smooth',
              block: 'start',
            });
          }
        });
      });
    </script>
  </body>
</html>
