---

export async function getStaticPaths() {
  return [
    { params: { lang: 'es' } }, 
    { params: { lang: 'en' } }, 
  ];
}

// Get the language from params
const { lang } = Astro.params;
return Astro.redirect(`/${lang}/portfolio/`, 302);
---