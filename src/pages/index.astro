---
// Redirect automático a la versión en español
// En producción usa el base path, en desarrollo no
return Astro.redirect(
  `${import.meta.env.PROD ? '/chrystian_portfolio' : ''}/es/`,
  301,
);
---
